using System;
using System.Collections.Generic;
using System.Text;

namespace Domain.Enums
{
    public enum ReesterProjectStatus
    {
        CUSTOM = 0,
        REGISTERED = 1,
        IN_REWORK = 2,
        IN_REVIEW = 3,
        REJECTED = 4,
        DISCARD = 5,
        NOT_EXIST = 6,
        CONFIRMED = 7,
        DELETED = 8
    }
}
